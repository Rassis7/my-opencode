---
description: Route Render questions and actions
---

Use the `render-router` skill for every request about Render.

If the request is operational, use the Render MCP tools.
If the request is research, search, or planning, consult `https://render.com/docs` first.
If the request mixes both, answer in two parts: what can be executed and what needs research.

Treat `$ARGUMENTS` as additional context for the request.
Respond in Portuguese.
